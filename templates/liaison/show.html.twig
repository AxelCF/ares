{% extends 'base.html.twig' %}

{% block title %}Liaison
{% endblock %}

{% block body %}
	<h1>Liaison</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ liaison.id }}</td>
				<th>professor</th>
				<td>{{ liaison.getProfessor }}</td>
				<th>subject</th>
				<td>{{ liaison.getSubject }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_liaison_index') }}">back to list</a>

	<a href="{{ path('app_liaison_edit', {'id': liaison.id}) }}">edit</a>

	{{ include('liaison/_delete_form.html.twig') }}
{% endblock %}
